[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "cd server && npm ci",
  "cd client && npm ci"
]

[phases.build]
cmds = [
  "cd server && npm run build",
  "cd client && npm run build"
]

[start]
cmd = "node server/dist/index.js"

[variables]
NODE_ENV = "production"
